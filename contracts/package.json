{
  "name": "@repo/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "compile": "forge build",
    "test": "forge test -vvv",
    "clean": "forge clean",
    "deploy": "forge build && npx ts-node scripts/deploy",
    "verify": "npx ts-node scripts/verify/checkContracts"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.3.0",
    "hardhat": "^3.0.1"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.1",
    "enquirer": "^2.4.1",
    "ethers": "^6.15.0",
    "fs-extra": "^11.3.1",
    "viem": "^2.34.0"
  }
}
